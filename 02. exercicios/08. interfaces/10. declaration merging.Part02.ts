interface User {
    id: number;
}

interface User {
    name: string;
    email: string;
}

const user1: User = {
    id: 1,
    name: "Jane Doe",
    email: "jane.doe@example.com",
};

console.log(user1);